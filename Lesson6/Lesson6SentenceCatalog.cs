namespace ukazure.llm.Lesson6;

internal static class Lesson6SentenceCatalog
{
    public static IReadOnlyList<string> All { get; } =
    [
        "az deployment group create with bicep",
        "az deployment group create with json",
        "az webapp deploy with package",
        "az webapp deploy with zip",
        "az containerapp up with source",
        "az containerapp up with yaml",
        "dotnet build src api project",
        "github actions deploy to appservice",
        "az functionapp publish with zip",
        "bicep build main template file"
    ];
}
